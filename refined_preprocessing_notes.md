# 精细化预处理说明

本轮预处理将缺失值、长尾指标和二值变量分别处理，目标是让每一步都由训练集统计和医学含义共同驱动，而不是简单地一刀切填中位数。

## 1. 缺失值分类处理

缺失值策略完全由训练集计算，并复用于测试集，避免数据泄露。

### Type A：缺失本身有医学/行为含义，填 0

这类字段缺失通常表示“未做该检查”或“不适用”，因此先加缺失指示，再把原始列填 0。

字段：`TnI峰值`、`CK-MB峰值`、`MYO峰值`、`TIMI危险评分（STEMI）`、`戒烟时间`、`戒酒年限`。

### Type B：低缺失、低标签差异，常规填补

判定规则：

- 训练集缺失率 `< 30%`
- 各标签缺失率最大差异 `< 10%`
- 不属于 Type A

填补方式：

- 二值特征：训练集众数
- 连续/多值特征：训练集中位数

### Type C：高缺失或标签差异明显，加指示再填中位数

除 Type A/B 之外的字段归为 Type C。它们的缺失模式本身可能携带诊断或分层信息，因此先加 `_missing` 指示，再用训练集中位数填补。

本轮结果：

```text
Type A: 6
Type B: 90
Type C: 41
新增缺失指示特征: 47
```

详细计划保存在 `preprocessed_outputs_v2/missing_imputation_plan.csv`。

## 2. 结构化缺失：组级检查行为特征

较完整 EDA 显示，约 20 多个生化指标共享接近 52.3% 的缺失模式，说明“是否做过生化检查”本身是一个结构化信号。因此在单列 `_missing` 指示之外，新增：

```text
has_biochem
biochem_panel_count
```

这两个特征分别表示是否至少完成一项生化检查，以及完成了多少项生化检查。它们在填补前创建，保留检测行为信息。

## 3. 默认删除列

根据 EDA、泄漏敏感性消融和训练集相关性检查，默认删除两类特征：

- endpoint-like 特征：`心源性死亡`、`卒中`、`靶血管重建`、`MACE事件`、`再发心梗`、`脑梗死`、`脑出血`、`急性支架内血栓`
- 完全重复列：`肝功全套`，它与 `生化C11` 逐行完全相同

`脉搏` 虽然与 `心率` 高度相关，但 ablation 显示删除它会轻微损伤少数类指标，因此正式 pipeline 中保留。`间接胆红素`、`低密度脂蛋白胆固醇` 等虽然与相关指标高度相关，但并非完全重复，且具有明确医学含义，因此也暂不默认删除。

## 4. 异常值 Winsorize

对训练集中 IQR 异常值比例 `> 5%` 的连续特征，用训练集 99 分位作为上限截断，再把同一上限应用到测试集。该步骤只改变原始连续列的极端上尾，不使用测试集分布。

本轮自动识别出 20 个 winsorize 特征，完整上限保存在 `preprocessed_outputs_v2/winsorization_caps.csv`。

## 5. 长尾指标处理：Winsorize + 先验交互中的压缩表达

对关键医学指标不再默认生成额外 `*_log` 或 `*_grade` 列。当前保留两类处理：

- 原始填补后数值：保留绝对量级，并对高异常值比例连续变量做 q99 winsorize
- 医学先验交互中的压缩表达：例如 `cardiac_triple_hit`、`cardiorenal_index`、`crp_albumin_ratio` 内部使用 `log1p`，但不额外扩展大量单变量 log 列

LightGBM 消融显示，`expanded_log` 与临床分级特征没有稳定收益，甚至会降低 macro-F1 和少数类召回。因此正式 pipeline 默认不再生成额外 log 特征和 clinical grade 特征；长尾问题主要通过 q99 winsorize 和医学先验交互特征处理。

## 6. 二值特征卡方检验

对训练集中 42 个二值特征做卡方检验，评估其与标签的关联强度。结果中 `p < 0.05` 的显著二值特征共 18 个。

Top 显著二值特征包括：

```text
呋塞米
螺内酯
利尿剂_x
慢性肾功能不全
NSTEMI
房颤_x
阿司匹林
ACEI_x
硝酸甘油
性别
```

完整结果保存在 `preprocessed_outputs_v2/binary_chi2_results.csv`，显著特征列表保存在 `preprocessed_outputs_v2/significant_binary_cols.csv`。

当前流水线默认**不直接删除**非显著二值特征，因为类别极不平衡且样本量很小，直接删除可能损失弱交互信号。更稳妥的做法是在后续 ablation 实验中比较“全量二值特征”与“仅显著二值特征”的 OOF 表现。

## 7. 暂缓默认启用的处理

### 低 MI 特征删除

全量 MI 普遍很低，说明数据总体信号弱。但在极端不平衡任务中，低单变量 MI 特征仍可能通过交互产生贡献。因此当前只落盘 MI/卡方结果，不默认删除 bottom-MI 特征。

## 8. 当前产物概览

```text
训练集: 18789 x 188
测试集: 5000 x 187
模型特征数: 186
默认删除特征: 9
新增 log 特征: 0
新增临床分级特征: 0
新增医学先验/检查行为特征: 11
Winsorize 特征: 20
填补后训练/测试缺失值: 0 / 0
```

## 报告可用表述

> 我们没有对所有缺失值统一填补，而是依据缺失率、类别间缺失差异和医学含义将特征分为三类：对“未检测即有诊断含义”的字段加缺失指示并填 0；对低缺失且低标签差异字段采用训练集众数/中位数；对高缺失或类别差异明显字段加缺失指示后使用训练集中位数填补。所有填补统计量均只从训练集估计，并复用于测试集，以避免数据泄露。

> 对长尾医学指标，我们先通过训练集 q99 分位进行温和 winsorize，避免极端值主导树模型分裂；额外的单变量 log 和临床分级在消融实验中没有稳定收益，因此不进入最终预处理。

> 对二值特征，我们使用卡方检验评估其与标签的关联强度，识别出 18 个 `p < 0.05` 的显著二值特征，并将结果用于后续特征筛选和消融实验。

> 根据 EDA 中的结构化缺失发现，我们额外构造 `has_biochem` 和 `biochem_panel_count` 来表示生化检查是否完成及完成强度，避免将“未检查”与“检查正常”简单等同。
