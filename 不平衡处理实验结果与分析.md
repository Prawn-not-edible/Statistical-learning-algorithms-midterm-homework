# 不平衡处理实验结果与分析

## 1. 实验目的

前一轮五个 baseline 模型的共同问题是：Log Loss 和 Accuracy 看起来不错，但 Macro F1 只有约 `0.1655`，少数类 `label=1~5` 基本没有被识别出来。

因此，本轮实验围绕“类别极端不平衡”做了三组补充实验：

- 实验1：TabPFN + 先验概率修正；
- 实验2：TabPFN + Class 1 合成数据增强；
- 实验3：LightGBM 二分类筛查 + TabPFN 少数类细分类的两阶段模型。

本轮结果来自云端 GPU 运行，使用增强预处理后的同一套训练/验证划分。

## 2. 最终结果汇总

| 实验组 | Log Loss | Macro F1 | Accuracy | Class 1 Recall | 少数类整体 Recall |
|---|---:|---:|---:|---:|---:|
| TabPFN 原始 baseline | 0.085399 | 0.165528 | 0.986429 | 0.000000 | 0.000000 |
| 最佳先验修正：`prior_grid_class1_0.05` | 0.392657 | 0.166754 | 0.955295 | 0.000000 | 0.117647 |
| 最佳合成增强：`synthetic_class1_50` | 0.085691 | 0.165528 | 0.986429 | 0.000000 | 0.000000 |
| 最佳两阶段模型：`two_stage_w50_t0.05` | 0.194258 | 0.232194 | 0.986429 | 0.000000 | 0.019608 |

如果以 `Macro F1` 作为最终指标，本轮最佳模型是：

```text
LightGBM Stage 1 + TabPFN Stage 2
配置：two_stage_w50_t0.05
Macro F1：0.232194
```

相比原始 TabPFN baseline：

```text
Macro F1: 0.165528 -> 0.232194
绝对提升: +0.066666
相对提升: 约 +40.3%
```

不过需要注意：虽然 Macro F1 提升明显，但少数类整体召回率仍然只有 `0.019608`，也就是验证集中大约只抓对了 `1/51` 个少数类样本。`Class 1 Recall` 仍然是 `0`。

## 3. 实验1：TabPFN + 先验概率修正

### 3.1 结果

最佳先验修正方案是：

```text
prior_grid_class1_0.05
Log Loss: 0.392657
Macro F1: 0.166754
Accuracy: 0.955295
Class 1 Recall: 0.000000
少数类整体 Recall: 0.117647
```

### 3.2 分析

先验修正的效果比较有代表性：

- 它确实让模型开始预测更多少数类，少数类整体 Recall 从 `0` 提升到 `0.117647`；
- 但 Macro F1 只从 `0.165528` 提升到 `0.166754`，几乎没有实质提升；
- Accuracy 从 `0.986429` 降到 `0.955295`；
- Log Loss 从 `0.085399` 明显变差到 `0.392657`。

这说明先验修正虽然能把模型从“全预测多数类”的状态中拉出来，但代价是误报明显增加，而且预测出来的少数类未必是正确类别。

也就是说，先验修正更像是一个“调高少数类敏感度”的方法，而不是一个已经足够好的最终分类策略。

### 3.3 结论

先验修正证明了一个重要现象：TabPFN 的概率里确实含有少数类信号，但直接改变先验会破坏概率校准，导致 Log Loss 大幅变差。它适合作为分析实验写进报告，但当前不适合作为最终模型。

## 4. 实验2：TabPFN + Class 1 合成数据增强

### 4.1 结果

最佳合成增强方案是：

```text
synthetic_class1_50
Log Loss: 0.085691
Macro F1: 0.165528
Accuracy: 0.986429
Class 1 Recall: 0.000000
少数类整体 Recall: 0.000000
```

### 4.2 分析

这组实验没有带来可见提升。它的 Macro F1、Accuracy 和少数类 Recall 基本与原始 TabPFN baseline 一样。

原因可能有两个：

- 当前脚本里的合成数据是可复现的统计扰动版本，并不是真正调用 LLM 按 EPIC prompt 生成的医学合理样本；
- Class 1 原始样本只有 5 条，验证集中 Class 1 只有 1 条，合成数据很难稳定覆盖真实 Class 1 的判别边界。

所以，这个结果不能说明“EPIC 思路无效”，只能说明“当前这版简单合成增强没有明显收益”。

### 4.3 结论

当前合成增强版本不建议作为最终模型依据。后续如果要继续做 EPIC，需要让 LLM 基于原始 Class 1 病历生成更有医学一致性的合成样本，而不是只做数值扰动。

## 5. 实验3：LightGBM + TabPFN 两阶段模型

### 5.1 结果

最佳两阶段模型是：

```text
two_stage_w50_t0.05
Stage 1: LightGBM 二分类，class_weight={0:1, 1:50}
Stage 1 阈值: 0.05
Stage 2: TabPFN 对 Stage 1 判为异常的样本做少数类细分
Log Loss: 0.194258
Macro F1: 0.232194
Accuracy: 0.986429
Class 1 Recall: 0.000000
少数类整体 Recall: 0.019608
```

### 5.2 分析

两阶段模型是本轮 Macro F1 最高的方案。它的优点是：

- Macro F1 从 `0.165528` 提升到 `0.232194`；
- Accuracy 没有下降，仍为 `0.986429`；
- 相比先验修正，它没有造成大量误报；
- 结构上更符合任务特点：先检测是否异常，再做少数类细分。

但它的缺点也很明显：

- Log Loss 比原始 TabPFN 差，说明概率质量变差；
- 少数类整体 Recall 只有 `0.019608`；
- Class 1 Recall 仍然为 `0`；
- 当前提升可能来自极少量少数类样本被正确识别，整体稳定性还需要交叉验证确认。

### 5.3 结论

如果当前阶段必须选一个“最终模型”，两阶段模型是最合理的选择，因为它是唯一显著提高 Macro F1 的方法。

但从业务解释上必须强调：这还不是一个真正解决少数类识别问题的模型，它只是相比多数类分类器迈出了一小步。

## 6. 当前最终模型选择

综合本轮实验，如果以课程任务中更适合不平衡分类的指标 `Macro F1` 为主，最终选择：

```text
最终模型：LightGBM Stage 1 + TabPFN Stage 2
最佳配置：two_stage_w50_t0.05
Macro F1：0.232194
Accuracy：0.986429
Log Loss：0.194258
```

选择理由：

- 它是本轮 Macro F1 最高的方案；
- 它没有牺牲 Accuracy；
- 它比单纯先验修正更稳定，误报更少；
- 它的结构更适合极端不平衡任务。

需要同时说明：

- 如果目标是概率预测质量，AutoGluon 和原始 TabPFN 的 Log Loss 更好；
- 如果目标是少数类识别和 Macro F1，两阶段模型目前更值得保留；
- Class 1 仍然没有被识别出来，后续还需要继续改进。

## 7. 可以写进正式报告的总结

在五个 baseline 模型均退化为多数类分类器之后，本文进一步尝试了三类不平衡处理方法。实验结果显示，TabPFN 的先验概率修正能够提高少数类整体召回率，但会显著降低 Accuracy 并恶化 Log Loss，说明直接改变类别先验会带来较多误报。Class 1 合成数据增强在当前简单统计扰动版本下没有明显收益，Macro F1 与原始 TabPFN 基本一致。相比之下，LightGBM + TabPFN 的两阶段模型取得了最高的 Macro F1，将指标从原始 TabPFN 的 0.1655 提升到 0.2322，同时保持 Accuracy 为 0.9864。因此，在当前实验范围内，两阶段模型是最优方案。不过，该模型的少数类整体召回率仍然较低，Class 1 Recall 仍为 0，说明极端少数类学习仍然是本任务的核心难点。

## 8. 下一步建议

后续如果继续优化，建议优先做以下几件事：

- 对两阶段模型做更细的阈值扫描，例如 `0.01 ~ 0.10` 之间更密集地找点；
- 对 Stage 1 使用更高召回导向的目标，例如固定少数类 Recall 后再最小化误报；
- 对 `label>0` 先做整体异常检测，再考虑少数类内部是否应该合并或重定义；
- 用交叉验证替代单次验证集，因为当前 Class 1 验证样本只有 1 条，单次结果非常不稳定；
- 如果继续做 EPIC，需要使用真实 LLM 生成的医学合理样本，而不是简单数值扰动。
